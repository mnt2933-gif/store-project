# خطوات التشغيل والرفع

## ١. التشغيل المحلي (على جهازك، زي CS50)
```
pip install -r requirements.txt
python app.py
```
افتحي المتصفح على: http://localhost:5000

لوحة التحكم: http://localhost:5000/admin

## ٢. الرفع على GitHub
```
git init
git add .
git commit -m "أول نسخة من الموقع"
```
بعدين سوي repository جديد على github.com وارفعي الكود له (GitHub بيوريكي الأوامر بالضبط وقت إنشاء الـ repo).

## ٣. النشر على Render (مجاني)
1. سجلي حساب على render.com
2. اختاري "New +" ثم "Web Service"
3. اربطيه بالـ GitHub repo اللي رفعتيه
4. في خانة **Start Command** اكتبي:
   ```
   gunicorn app:app
   ```
5. اضغطي Deploy، وبعد دقائق يعطيك رابط زي:
   `https://storename.onrender.com`

هذا الرابط هو رابط الديمو اللي ترسليه للمحلات.

## ملاحظة مهمة
قاعدة بيانات SQLite على الباقة المجانية من Render ممكن تنمسح لو السيرفر أعاد التشغيل
(لأنه تخزين مؤقت). هذا مقبول لمرحلة الديمو. لو صار عندك عميل حقيقي وبدأ يستخدم
الموقع بجد، ننتقل وقتها لاستضافة مدفوعة بسيطة تحفظ البيانات بشكل دائم.
